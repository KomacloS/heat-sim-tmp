diff --git a/laserpad/geometry.py b/laserpad/geometry.py
index e.g.1234..f.56789 100644
--- a/laserpad/geometry.py
+++ b/laserpad/geometry.py
@@ -22,7 +22,8 @@ def build_mesh(r_inner: float, r_outer: float, n_r: int = 100) -> fp.Grid1D:
-    dr = (r_outer - r_inner) / n_r
-    # Shift origin such that first cell center sits at r_inner:
-    origin = (r_inner - 0.5 * dr,)
-    mesh = fp.Grid1D(nx=n_r, dx=dr, origin=origin)
+    dr = (r_outer - r_inner) / n_r
+    # Build a 1D radial mesh (cell-width = dr)
+    mesh = fp.Grid1D(nx=n_r, dx=dr)
+
+    # Remember true radii so solver can recover absolute positions
+    mesh._r_inner = r_inner
+    mesh._r_outer = r_outer
